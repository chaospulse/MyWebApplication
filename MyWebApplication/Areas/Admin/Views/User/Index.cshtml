@*-----CODE WITHOUT JAVA SCRIPT-----*@

@*@model List<PulseModels.Models.ViewModels.RoleManagmentVM>
<div class="card shadow border-0 bg-gradient ml-0 mt-4">
	<div class="card-header shadow bg-secondary bg-gradient ml-0 py-3">
	     <div class="row">
			 <div class="col-12 col-md-6">
				 <h2 class="text-light">User List</h2>
			 </div>
		 </div>
	</div>

   <div class="card-body p-2">
	
	  <table class="table table-dark table-bordered" >
	       <thead> 
				 <tr>
					 <th>Name</th>
					 <th>Email</th>
					 <th>Phone</th>
					 <th>Company</th>
				     <th></th>
				 </tr>
	        </thead>
			@foreach (var item in Model)
			{
				<tr>
					<td>@item.ApplicationUser.Name</td>
					<td>@item.ApplicationUser.Email</td>
					<td>@item.ApplicationUser.PhoneNumber</td>
					<td>@item.ApplicationUser.Company</td>
					<th>@item.ApplicationUser.Role</th>
					<td>
						<div class="w-75 btn-group" role="group">
						<a asp-controller="Category" asp-action="Edit" asp-route-id="@item.ApplicationUser.Id" class="btn btn-outline-light mx-1"> 
						   <i class="bi bi-pencil-square"></i> Edit
						</a>
						<a asp-controller="Category" asp-action="Delete" asp-route-id="@item.ApplicationUser.Id" class="btn btn-outline-danger mx-1"> 
						   <i class="bi bi-trash"></i> Delete
						</a>
					</div></td>
				</tr>
			}
      </table>
   </div>
</div>*@

@*-----CODE WITH JAVA SCRIPT-----*@

﻿<div class="card shadow border-0 my-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">User List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <table id="userTable" class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Company</th>
                    <th>Role</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/user.js"></script>
}
